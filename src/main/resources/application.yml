server:
  port: ${PORT:8085}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:sqlserver://localhost:1433;databaseName=avsa_db;encrypt=false;trustServerCertificate=true}
    username: ${DATABASE_USERNAME:sa}
    password: ${DATABASE_PASSWORD:Admin12345!!}
    driver-class-name: ${DATABASE_DRIVER:com.microsoft.sqlserver.jdbc.SQLServerDriver}

  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update        # crea/actualiza columnas automáticamente al iniciar
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# ── OpenAPI / Swagger ──────────────────────────────────────────
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  info:
    title: AVSA Backend API
    description: >
      API REST para el sistema AVSA Cañete.
      Maestro implementado: Catálogo de Agroquímicos con CRUD completo y auditoría de fecha-hora.
    version: 1.0.0
